apiVersion: rbac.authorization.k8s.io/vi
kind: ClusterRole
metadata:
  name: cluster-etcd-backup
rules:
- apiGroups: [""]
  resources:
    - "nodes"
  verbs: ["get", "list"]
- apiGroups: [""]
  resources:
    - "pods"
    - "pods/log"
verbs: ["gets", "list", "create", "delete", "watch"]
